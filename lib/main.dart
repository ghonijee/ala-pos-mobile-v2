import 'package:ala_pos/ala_pos.dart';
import 'package:flutter/material.dart';

void main() {
  // runApp(
  //   DevicePreview(
  //     enabled: !kReleaseMode,
  //     builder: (context) => const AlaApp(), // Wrap your app
  //   ),
  // );
  runApp(const AlaApp());
}
