import 'base_color.dart';

class AppColors {
  // Backgorund
  static const backgroundDark = BaseColor.brandPrimary;
  static const backgroundLight = BaseColor.whitePrimary;

  static const sidebarLight = BaseColor.brandAccent6;
  static const sidebarDark = BaseColor.brandAccent6;

  static const primary = BaseColor.brandPrimary;
  static const accent = BaseColor.brandAccent5;
  static const tertiary = BaseColor.brandAccent3;
  static const surface = BaseColor.whiteTrue;

  static const enableBorderDark = BaseColor.whitePrimary;
  static const enableBorderLight = BaseColor.greyPrimary;
  static const focusBorderDark = BaseColor.whiteTrue;
  static const focusBorderLight = BaseColor.brandAccent6;

  // Button
  static const buttonPrimary = BaseColor.brandPrimary;
  static const buttonSecondary = BaseColor.brandBackground;
  static const buttonAccent = BaseColor.brandAccent4;

  // Text
  static const textDefaultLight = BaseColor.blackPrimary;
  static const textDefaultDark = BaseColor.whitePrimary;
  static const textPrimaryDark = BaseColor.brandBackground;
  static const textPrimaryLight = BaseColor.brandAccent6;
  static const textSecondaryLight = BaseColor.greyPrimary;
  static const textSecondaryDark = BaseColor.whiteAccent2;

  // Status
  static const errorLight = BaseColor.brandAccent2;

  // AppBar
  static const iconLight = BaseColor.whitePrimary;
}
